# 安全政策

## 支持范围

安全修复以默认分支的最新版本为准。旧版本不会单独维护安全补丁。

## 报告安全问题

请不要在公开 Issue 中披露可利用的漏洞、API Key 或用户数据。请通过 GitHub 仓库的“Security”页面创建私密漏洞报告，并提供：

- 受影响的版本或提交。
- 可复现步骤与预期影响。
- 必要的最小测试材料；不要附真实密钥或第三方隐私数据。
- 你已经尝试过的缓解方式。

## 本地安全边界

项目默认监听 `127.0.0.1`，面向单机使用。`.model-api-settings.json` 包含可用的供应商密钥，即使文件权限受限，也不应上传、分享或备份到不可信位置。主动修改 `HOST` 将服务暴露到其他设备时，部署者需要自行增加身份验证、网络访问控制、TLS 和请求审计。
